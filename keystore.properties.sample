# Copy this file to keystore.properties (same folder) and fill in your real
# values. keystore.properties is git-ignored — never commit real credentials.
storeFile=relayiq-upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=relayiq-upload
keyPassword=CHANGE_ME
